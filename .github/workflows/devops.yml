name: Python application test with Github Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.9.19
        uses: actions/setup-python@v1
        with:
          python-version: 3.9.19

      - name: Install dependencies
        run: |
          make install
          python -m textblob.download_corpora

      - name: Lint with pylint
        run: echo "Placeholder for pylint" # Replace this line with `make lint` when ready

      - name: Test with pytest
        run: echo "Placeholder for pytest" # Replace this line with `make test` when ready

      - name: Format code
        run: echo "Placeholder for code formatting" # Replace this line with `make format` when ready

      - name: Build Container
        run: |
          make build
